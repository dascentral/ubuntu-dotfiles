#!/bin/sh

#===================================================================================
# Laravel - Create Storage Folder
#===================================================================================
#
# DESCRIPTION
# Script that recreates the Laravel "storage" folder structure and adjust
# permissions if specified.
#
# PARAMETERS
# - Username - Optional - User granted recursive ownership of the storage folder
#
#===================================================================================

# Create the directory structure
mkdir -p storage
mkdir -p storage/app
mkdir -p storage/framework/cache
mkdir -p storage/framework/sessions
mkdir -p storage/framework/views
mkdir -p storage/logs

# Set proper permissions
sudo chown -R ${USER} storage
sudo chgrp -R www-data storage
sudo chmod -R 775 storage
